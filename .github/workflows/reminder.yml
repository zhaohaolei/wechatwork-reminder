name: 晚会提醒机器人
on:
  schedule:
    - cron: '0 9 * * 1,3,5'
  workflow_dispatch:

jobs:
  send_message:
    runs-on: ubuntu-latest
    steps:
      - name: 发送企微消息
        run: |
          curl -X POST "https://qyapi.weixin.qq.com/cgi-bin/webhook/send?key=acd06db3-bafa-4e76-ad4d-2aeb13a90973" \
          -H "Content-Type: application/json" \
          -d '{
            "msgtype": "markdown",
            "markdown": {
              "content": "### 📢 晚会提醒\n各位同事，现在是17:00，今晚的团队晚会即将开始，请大家做好准备～\n> 推送时间: '"$(date +'%Y-%m-%d %H:%M:%S')"'"
            }
          }'
