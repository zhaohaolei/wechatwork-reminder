name: 晚会提醒机器人
on:
  # 定时触发：每周一、三、五 16:55（东八区）
  # GitHub是UTC时区，东八区16:55 = UTC 8:55，对应cron格式：55 8 * * 1,3,5
  schedule:
    - cron: '55 8 * * 1,3,5'
  # 手动触发（方便测试）
  workflow_dispatch:

jobs:
  send_message:
    runs-on: ubuntu-latest
    steps:
      - name: 发送企微消息
        run: |
          curl -X POST "https://qyapi.weixin.qq.com/cgi-bin/webhook/send?key=1fa62b6b-cffb-42ad-820b-f63c649381ca" \
          -H "Content-Type: application/json" \
          -d '{
            "msgtype": "markdown",
            "markdown": {
              "content": "### 📢 晚会提醒\n各位同事，现在是16:55，今晚的团队晚会即将开始，请大家做好准备～\n> <@liujianlong@cvte.com>、<@pengdi@cvte.com>、<@zhaohaolei@cvte.com> \n> 推送时间: '"$(date +'%Y-%m-%d %H:%M:%S')"'"
            },
            "mentioned_list": ["liujianlong@cvte.com", "pengdi@cvte.com", "zhaohaolei@cvte.com"]
          }'
